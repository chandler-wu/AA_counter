# 团队记账应用 - 开发计划

> 配套文档：项目需求.md
> 日期：2026-06-07

## 一、目标

实现一个用于团队记账的纯前端 SPA：支持多项目并行、成员/分类管理、按笔记账、按项目统计（总额、分类、人均、个人排行榜）。

## 二、确认的关键决策

| 维度 | 决策 |
|---|---|
| 技术栈 | Vite + React 18 + TypeScript |
| UI 库 | Ant Design 5.x（中文） |
| 状态管理 | React Context + useReducer |
| 持久化 | LocalStorage（单一 key `aa_counter_v1`） |
| 路由 | React Router 6 |
| 金额 | number 类型，2 位小数，前缀 ¥ |
| 分摊 | **仅记录，不做结算**（不计算"应付/应收"） |
| 排行榜 | 按**参与金额**降序 |
| 项目范围 | 支持多项目并行 |
| 默认分类 | 交通、餐饮、住宿、其他 |
| 默认成员 | 成员1/2/3 |
| 货币/语言 | 人民币 / 中文 |

## 三、数据模型

```ts
// src/types.ts
export interface Member { id: string; name: string }
export interface Category { id: string; name: string; color: string }
export interface Expense {
  id: string;
  amount: number;
  categoryId: string;
  payerId: string;
  participantIds: string[];
  description: string;
  date: string;        // YYYY-MM-DD
  createdAt: string;   // ISO
}
export interface Project {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
  members: Member[];
  categories: Category[];
  expenses: Expense[];
}
export interface AppState {
  version: 1;
  projects: Project[];
}
```

## 四、目录结构

```
AA_counter/
├── package.json
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
├── index.html
├── 项目需求.md
├── 项目开发计划.md
└── src/
    ├── main.tsx
    ├── App.tsx
    ├── types.ts
    ├── storage.ts
    ├── seed.ts
    ├── utils/
    │   ├── id.ts
    │   ├── money.ts
    │   └── stats.ts
    ├── state/
    │   ├── reducer.ts
    │   └── AppContext.tsx
    ├── pages/
    │   ├── ProjectList.tsx
    │   ├── ProjectHome.tsx
    │   ├── ExpenseList.tsx
    │   └── StatsPage.tsx
    └── components/
        ├── ProjectCard.tsx
        ├── MemberPanel.tsx
        ├── CategoryPanel.tsx
        ├── ExpenseForm.tsx
        ├── ExpenseTable.tsx
        ├── CategoryStatTable.tsx
        └── PersonRanking.tsx
```

## 五、路由

| 路径 | 页面 |
|---|---|
| `/` | ProjectList（项目列表） |
| `/projects/:id` | ProjectHome（项目主页 Tabs） |
| `/projects/:id/expenses` | ExpenseList（记账列表 + 新增） |
| `/projects/:id/stats` | StatsPage（统计） |

## 六、Action 列表（reducer）

- `INIT` — 启动时从 LocalStorage 加载
- `CREATE_PROJECT(name)`
- `DELETE_PROJECT(id)`
- `RENAME_PROJECT(id, name)`
- `ADD_MEMBER(projectId, name)`
- `RENAME_MEMBER(projectId, id, name)`
- `DELETE_MEMBER(projectId, id)`
- `ADD_CATEGORY(projectId, name, color)`
- `RENAME_CATEGORY(projectId, id, name, color)`
- `DELETE_CATEGORY(projectId, id)`
- `ADD_EXPENSE(projectId, expense)`
- `UPDATE_EXPENSE(projectId, expense)`
- `DELETE_EXPENSE(projectId, id)`

## 七、统计计算（纯函数 `utils/stats.ts`）

```ts
calcTotal(expenses): number
calcCategoryStats(expenses, categories): {categoryId, amount, count, percent}[]
calcPersonStats(expenses, members): {memberId, name, amount, count}[]   // 参与金额
calcPerCapita(total, memberCount): number
```

**孤立 ID 处理**：被删除的 member/category 仍可能出现在历史 expense 中，统计时按"找不到则跳过"过滤，UI 表格里显示"（已删除）"。

## 八、实施步骤

1. 初始化项目骨架（package.json / tsconfig / vite.config / index.html）
2. 实现数据层：types / storage / seed
3. 实现工具层：id / money / stats
4. 实现状态层：reducer / AppContext
5. 实现共享组件：表单、表格、面板
6. 实现页面：项目列表 / 项目主页 / 记账 / 统计
7. 入口与路由：main.tsx / App.tsx
8. 装包并启动验证

## 九、验证方案

1. `npm install` 0 错误
2. `npm run dev` 启动，控制台无 error
3. 浏览器打开 http://localhost:5173
4. 新建项目"北京旅行" → 自动有 3 个成员、4 个分类
5. 添加消费：100 元 / 餐饮 / 成员1 付 / 三人均参与
6. 再添加：200 元 / 交通 / 成员2 付 / 成员2、3 参与
7. 统计页：总额 300、人均 100、分类 2 条；排行榜：成员2=300、成员3=300、成员1=100
8. 改名、重命名、删除项后所有页面同步
9. 刷新浏览器 → 数据保留
10. `npm run build` 产出 dist，无 TS 错误

## 十、范围外（明确不做）

- 结算/应收应付
- 数据导出/导入
- 多设备同步
- 用户登录/权限
- 图表库（用 Ant Design Table + Progress 已足够）
- 移动端响应式（桌面优先，不专门做小屏适配）
